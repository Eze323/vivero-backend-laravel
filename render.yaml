services:
  - type: web
    name: vivero-backend-laravel
    runtime: php
    env: php
    buildCommand: |
      composer install --no-dev --optimize-autoloader --no-interaction
      php artisan key:generate
      php artisan config:cache
    startCommand: php artisan serve --host 0.0.0.0 --port $PORT
    plan: free
    region: oregon
    phpVersion: 8.2
